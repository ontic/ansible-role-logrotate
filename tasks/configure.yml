# This source file is subject to the (Open Source Initiative) BSD license
# that is bundled with this package in the LICENSE file. It is also available
# through the world-wide-web at this URL: http://www.ontic.com.au/license.html
# If you did not receive a copy of the license and are unable to obtain it through
# the world-wide-web, please send an email to license@ontic.com.au immediately.
# Copyright (c) 2010-2016 Ontic. (http://www.ontic.com.au). All rights reserved.

---

- name: 'Logrotate | Configure rotation config files.'
  become: yes
  template:
    src: '{{ item.template | default(logrotate_config_template) }}'
    dest: '{{ logrotate_conf_path }}/{{ item.name }}'
    state: '{{ item.state | default(omit) }}'
    owner: 'root'
    group: 'root'
    mode: '0644'
  with_items: '{{ logrotate_config_files }}'
  when: '{{ logrotate_config_files | default(None) != None }}'

- name: 'Logrotate | Force file rotation.'
  become: yes
  command: 'logrotate --force {{ logrotate_conf_path }}/{{ item.name }}'
  with_items: '{{ logrotate_config_files }}'
  when: '{{ logrotate_config_files | default(None) != None and (logrotate_force_all or item.force is defined and item.force) }}'